# 🚀 防止Git冲突 - 快速使用指南

## ✅ 当前状态（所有文件正常）

```
✅ index.html - 紫色主题 (8处)
✅ 2026-01-24.html - 紫色主题 (9处)
✅ 2026-01-25.html - 紫色主题 (9处)
```

---

## 🛡️ 使用保护工具

### 1️⃣ 推送前检查（推荐）

**替代 `git push`：**
```bash
./scripts/safe_push.sh origin main
```

**功能：**
- ✅ 检查index.html是否是紫色主题
- ✅ 检查简报HTML是否正确
- ✅ 防止推送错误主题的文件

### 2️⃣ 随时检查主题

```bash
./scripts/check_theme.sh
```

**输出：**
```
✅ index.html - 紫色主题 (8处)
✅ 2026-01-24.html - 紫色主题 (9处)
...
```

---

## 📋 每日推荐流程

### 🌙 晚上/早上生成（避免冲突）

```bash
# 1. 生成简报（带去重）
python3 scripts/generate_with_dedup.py

# 2. 查看预览
cat preview/2026-01-26.md

# 3. 转换HTML
python3 scripts/md_to_purple_html.py output/archive/2026-01-26.md > 2026-01-26.html

# 4. 复制到各位置
cp 2026-01-26.html archive/
cp 2026-01-26.html output/archive/

# 5. 检查主题（重要！）
./scripts/check_theme.sh

# 6. 安全推送
./scripts/safe_push.sh origin main
```

### 🤖 如果使用GitHub Actions自动运行

**问题：** Actions会在每天8点自动运行，可能覆盖你的修改

**解决方案A：禁用自动运行（推荐）**
```yaml
# .github/workflows/daily-auto.yml
# 在第6行注释掉schedule触发器
# schedule:
#   - cron: '0 0 * * *'
```

**解决方案B：只手动触发**
- 保留workflow_dispatch
- 删除schedule
- 需要时在GitHub网页上手动点击"Run workflow"

---

## ⚠️ 如果GitHub Actions覆盖了主题

### 快速恢复命令

```bash
# 1. 检查哪些文件被覆盖
./scripts/check_theme.sh

# 2. 恢复index.html
git show 365d0a3:index.html > index.html

# 3. 恢复25号
git show afe2f81:2026-01-25.html > 2026-01-25.html
cp 2026-01-25.html archive/
cp 2026-01-25.html output/archive/

# 4. 再次检查
./scripts/check_theme.sh

# 5. 安全推送
./scripts/safe_push.sh origin main
```

---

## 🔒 长期保护（可选）

### 方案A：锁定index.html

```bash
# 告诉Git不要追踪index.html的更改
git update-index --assume-unchanged index.html

# 如果需要修改index.html
git update-index --no-assume-unchanged index.html
# 修改后重新锁定
git update-index --assume-unchanged index.html
```

**优点：** GitHub Actions不会覆盖你修改的index.html
**缺点：** 你手动修改index.html后需要重新锁定

### 方案B：禁用GitHub Actions自动运行

编辑 `.github/workflows/daily-auto.yml`，注释掉第5-6行：
```yaml
# on:
#   schedule:
#     - cron: '0 0 * * *'
```

只保留手动触发（第8-9行）。

---

## 📊 方案对比

| 方案 | 安全性 | 便利性 | 推荐度 |
|------|--------|--------|--------|
| 使用safe_push.sh | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ✅ 最推荐 |
| 锁定index.html | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ✅ 推荐 |
| 禁用自动运行 | ⭐⭐⭐⭐⭐ | ⭐⭐ | 可选 |
| 完全手动 | ⭐⭐⭐⭐⭐ | ⭐ | 不推荐 |

---

## 🎯 我的推荐

**最佳实践组合：**

1. ✅ **日常使用** `./scripts/safe_push.sh origin main`
2. ✅ **偶尔检查** `./scripts/check_theme.sh`
3. ✅ **禁用GitHub Actions自动运行**（避免意外覆盖）

这样你可以：
- 完全控制发布时间
- 避免被自动化覆盖
- 保持紫色主题一致

---

## 📞 需要帮助？

如果遇到问题：
1. 先运行 `./scripts/check_theme.sh` 检查状态
2. 查看 `GIT_CONFLICT_SOLUTIONS.md` 详细解决方案
3. 使用恢复命令快速修复

**记住：紫色主题代码是 `#667eea`（出现8-9次为正常）**
